// cargo run --bin unit_tests
// unit is the default return type if there is no type given
fn main() {
    let result=empty_function();
    dbg!(result);
}

fn empty_function() {
    // do nothing
}

